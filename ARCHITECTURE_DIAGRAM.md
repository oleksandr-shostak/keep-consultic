# OpenAI Assistant Provider - Architecture Diagram

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              KEEP PLATFORM                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────┐      ┌──────────────┐      ┌──────────────┐        │
│  │              │      │              │      │              │        │
│  │   Workflow   │─────▶│   Provider   │─────▶│  Assistant   │        │
│  │   Engine     │      │   Manager    │      │   Provider   │        │
│  │              │      │              │      │              │        │
│  └──────────────┘      └──────────────┘      └──────┬───────┘        │
│                                                       │                │
└───────────────────────────────────────────────────────┼────────────────┘
                                                        │
                                                        │ HTTPS
                                                        │
                                            ┌───────────▼──────────────┐
                                            │   OpenAI Platform       │
                                            │   api.openai.com        │
                                            ├─────────────────────────┤
                                            │                         │
                                            │  ┌──────────────────┐  │
                                            │  │   Your           │  │
                                            │  │   Assistant      │  │
                                            │  │   (asst_xxx)     │  │
                                            │  │                  │  │
                                            │  │  • Model:        │  │
                                            │  │    gpt-4o-mini   │  │
                                            │  │  • Instructions  │  │
                                            │  │  • Tools:        │  │
                                            │  │    - File Search │  │
                                            │  │    - Code Int.   │  │
                                            │  └──────────────────┘  │
                                            │                         │
                                            │  ┌──────────────────┐  │
                                            │  │  Knowledge Base  │  │
                                            │  │  (Vector Store)  │  │
                                            │  │                  │  │
                                            │  │  • Runbooks      │  │
                                            │  │  • Incidents     │  │
                                            │  │  • Docs          │  │
                                            │  └──────────────────┘  │
                                            │                         │
                                            └─────────────────────────┘
```

---

## 🔄 Request Flow

```
1. ALERT RECEIVED
   ┌────────────┐
   │   Alert    │  "Server CPU at 100%"
   └──────┬─────┘
          │
          ▼
2. WORKFLOW TRIGGERED
   ┌────────────────────┐
   │  Workflow Engine   │  Check triggers & filters
   └──────┬─────────────┘
          │
          ▼
3. PROVIDER CALLED
   ┌──────────────────────────────────┐
   │  OpenAI Assistant Provider       │
   │  _query(prompt, thread_id=None)  │
   └──────┬───────────────────────────┘
          │
          ▼
4. THREAD CREATED (or reused)
   ┌────────────────────┐
   │  OpenAI API:       │
   │  threads.create()  │
   └──────┬─────────────┘
          │
          ▼
5. MESSAGE ADDED
   ┌──────────────────────────────┐
   │  OpenAI API:                 │
   │  threads.messages.create()   │
   │  "Analyze this alert: ..."   │
   └──────┬───────────────────────┘
          │
          ▼
6. RUN STARTED
   ┌────────────────────────┐
   │  OpenAI API:           │
   │  threads.runs.create() │
   └──────┬─────────────────┘
          │
          ▼
7. ASSISTANT PROCESSES
   ┌──────────────────────────────────┐
   │  Assistant:                      │
   │  • Reads system instructions     │
   │  • Searches knowledge base       │
   │  • Runs code if needed           │
   │  • Generates response            │
   └──────┬───────────────────────────┘
          │
          ▼
8. POLLING FOR COMPLETION
   ┌────────────────────────────────┐
   │  Loop every 1s:                │
   │  threads.runs.retrieve()       │
   │  if status == "completed":     │
   │     break                      │
   └──────┬─────────────────────────┘
          │
          ▼
9. RESPONSE EXTRACTED
   ┌─────────────────────────────────┐
   │  OpenAI API:                    │
   │  threads.messages.list()        │
   │  Extract assistant's message    │
   └──────┬──────────────────────────┘
          │
          ▼
10. RESPONSE PARSED
   ┌──────────────────────────────┐
   │  Provider:                   │
   │  • Try JSON parse            │
   │  • Return dict with:         │
   │    - response                │
   │    - thread_id               │
   │    - run_id                  │
   │    - messages                │
   └──────┬───────────────────────┘
          │
          ▼
11. WORKFLOW CONTINUES
   ┌───────────────────────────────┐
   │  Next step can:               │
   │  • Use response.action        │
   │  • Continue thread            │
   │  • Escalate/resolve           │
   └───────────────────────────────┘
```

---

## 🧩 Component Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                OpenAI Assistant Provider Class                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Properties:                                                    │
│  ├─ authentication_config                                       │
│  │  ├─ api_key                                                  │
│  │  ├─ organization_id                                          │
│  │  └─ assistant_id                                             │
│  └─ client (OpenAI client instance)                            │
│                                                                 │
│  Methods:                                                       │
│  ├─ __init__(context_manager, provider_id, config)            │
│  ├─ validate_config()          → Verify assistant exists       │
│  ├─ validate_scopes()          → Check permissions             │
│  ├─ _query(prompt, ...)        → Main query method             │
│  │   ├─ Create/get thread                                      │
│  │   ├─ Add message                                            │
│  │   ├─ Start run                                              │
│  │   ├─ Poll for completion                                    │
│  │   ├─ Extract response                                       │
│  │   └─ Return dict                                            │
│  └─ dispose()                   → Cleanup                      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔀 Data Flow

```
INPUT (Workflow)
┌────────────────────────────────────┐
│ provider:                          │
│   type: openaiassistant            │
│   config: "{{ providers.my }}"     │
│   with:                            │
│     prompt: "Analyze alert..."     │
│     thread_id: "thread_abc"        │  (optional)
│     max_wait_time: 60              │  (optional)
└────────────────┬───────────────────┘
                 │
                 ▼
PROCESSING (Provider)
┌────────────────────────────────────┐
│ Authentication:                    │
│  ✓ api_key                         │
│  ✓ assistant_id                    │
│                                    │
│ Thread Management:                 │
│  • Create if no thread_id          │
│  • Reuse if thread_id provided     │
│                                    │
│ Message Handling:                  │
│  • Add user message to thread      │
│  • Start assistant run             │
│                                    │
│ Polling Loop:                      │
│  • Check status every 1s           │
│  • Handle errors/timeouts          │
│  • Wait for completion             │
│                                    │
│ Response Extraction:               │
│  • Get messages from thread        │
│  • Find last assistant message     │
│  • Extract text content            │
│  • Try JSON parse (if enabled)     │
└────────────────┬───────────────────┘
                 │
                 ▼
OUTPUT (To Workflow)
┌────────────────────────────────────┐
│ {                                  │
│   "response": {                    │
│     "action": "escalate",          │
│     "reason": "...",               │
│     "severity": "critical"         │
│   },                               │
│   "thread_id": "thread_abc123",    │
│   "run_id": "run_xyz789",          │
│   "messages": [...],               │
│   "raw_response": "..."            │
│ }                                  │
└────────────────┬───────────────────┘
                 │
                 ▼
USAGE (Next Workflow Steps)
┌────────────────────────────────────┐
│ • Access: steps.X.results.response │
│ • Continue thread: thread_id       │
│ • Conditional logic: action        │
│ • Enrichment: reason, severity     │
└────────────────────────────────────┘
```

---

## 🔄 Thread Lifecycle

```
SCENARIO 1: New Investigation
┌───────────────────────────────────────────────────────────┐
│                                                           │
│  Step 1: Initial Query (no thread_id)                    │
│  ┌─────────────────────────────────────────┐            │
│  │ Provider creates NEW thread              │            │
│  │ thread_id = "thread_abc123"              │            │
│  └─────────────────────────────────────────┘            │
│                     │                                     │
│                     ▼                                     │
│  Step 2: Follow-up Query (with thread_id)               │
│  ┌─────────────────────────────────────────┐            │
│  │ Provider reuses thread_abc123            │            │
│  │ Assistant has context from Step 1        │            │
│  └─────────────────────────────────────────┘            │
│                     │                                     │
│                     ▼                                     │
│  Step 3: Another Follow-up (with thread_id)             │
│  ┌─────────────────────────────────────────┐            │
│  │ Provider reuses thread_abc123            │            │
│  │ Full conversation context maintained     │            │
│  └─────────────────────────────────────────┘            │
│                                                           │
└───────────────────────────────────────────────────────────┘

SCENARIO 2: Parallel Investigations
┌───────────────────────────────────────────────────────────┐
│                                                           │
│  Alert A → Step 1 (no thread_id) → thread_aaa           │
│         → Step 2 (thread_aaa)     → continues            │
│                                                           │
│  Alert B → Step 1 (no thread_id) → thread_bbb           │
│         → Step 2 (thread_bbb)     → continues            │
│                                                           │
│  Threads are ISOLATED - no cross-contamination           │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

---

## ⚡ State Machine

```
RUN STATUS STATES
┌─────────────────────────────────────────────────────────┐
│                                                         │
│      queued ──▶ in_progress ──▶ completed              │
│                     │                                   │
│                     ├──▶ requires_action                │
│                     │    (function calling)             │
│                     │                                   │
│                     ├──▶ failed                         │
│                     │                                   │
│                     ├──▶ cancelled                      │
│                     │                                   │
│                     └──▶ expired                        │
│                                                         │
│  Provider Handling:                                     │
│  • "completed"       → Extract response, return         │
│  • "queued"          → Keep polling                     │
│  • "in_progress"     → Keep polling                     │
│  • "requires_action" → Log warning, keep polling        │
│  • "failed"          → Raise ProviderException          │
│  • "cancelled"       → Raise ProviderException          │
│  • "expired"         → Raise ProviderException          │
│  • timeout           → Cancel run, raise exception      │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 🎯 Decision Tree: Which Provider to Use?

```
                    ┌─────────────────┐
                    │  Need AI for    │
                    │  Alert Triage?  │
                    └────────┬────────┘
                             │
              ┌──────────────┴──────────────┐
              │                             │
        ┌─────▼──────┐              ┌──────▼─────┐
        │  Need       │              │  Simple    │
        │  Knowledge  │              │  Fast      │
        │  Base?      │              │  Triage?   │
        └─────┬──────┘              └──────┬─────┘
              │                             │
         ┌────▼────┐                        │
         │   YES   │                        │
         └────┬────┘                        │
              │                             │
      ┌───────▼────────┐                   │
      │  OpenAI        │                   │
      │  ASSISTANT     │                   │
      │  Provider      │                   │
      │                │                   │
      │  Features:     │                   │
      │  • File Search │              ┌────▼────┐
      │  • Code Exec   │              │   NO    │
      │  • Threads     │              └────┬────┘
      │  • Context     │                   │
      └────────────────┘          ┌────────▼────────┐
                                  │  Need LOCAL     │
                                  │  Model?         │
                                  └────────┬────────┘
                                           │
                              ┌────────────┴────────────┐
                              │                         │
                         ┌────▼────┐              ┌────▼────┐
                         │   YES   │              │   NO    │
                         └────┬────┘              └────┬────┘
                              │                        │
                    ┌─────────▼─────────┐    ┌────────▼────────┐
                    │  LiteLLM or       │    │  OpenAI         │
                    │  llamacpp         │    │  Provider       │
                    │  Provider         │    │                 │
                    │                   │    │  Features:      │
                    │  Features:        │    │  • Fast         │
                    │  • Free           │    │  • JSON         │
                    │  • Private        │    │  • Reliable     │
                    │  • On-premise     │    │  • Simple       │
                    └───────────────────┘    └─────────────────┘
```

---

## 📊 Performance Comparison

```
RESPONSE TIME COMPARISON
┌─────────────────────────────────────────────────────────┐
│                                                         │
│  OpenAI Provider:                                       │
│  ▓░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░ 1-2 seconds   │
│                                                         │
│  OpenAI Assistant (No Tools):                          │
│  ▓▓▓▓▓░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░ 5-7 seconds    │
│                                                         │
│  OpenAI Assistant (File Search):                       │
│  ▓▓▓▓▓▓▓▓░░░░░░░░░░░░░░░░░░░░░░░░░░░░ 8-12 seconds    │
│                                                         │
│  OpenAI Assistant (Code Interpreter):                  │
│  ▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░░░░░░░░░░░░░░░░░░ 12-20 seconds    │
│                                                         │
└─────────────────────────────────────────────────────────┘

COST COMPARISON (per 1000 alerts)
┌─────────────────────────────────────────────────────────┐
│                                                         │
│  Local Llama2:                                          │
│  $ (FREE)                                               │
│                                                         │
│  OpenAI (gpt-4o-mini):                                  │
│  $$ (~$0.15)                                            │
│                                                         │
│  OpenAI Assistant (gpt-4o-mini + File Search):         │
│  $$$ (~$0.20 + $3/month storage)                       │
│                                                         │
│  OpenAI Assistant (gpt-4o + all tools):                │
│  $$$$ (~$2.50 + $3/month storage)                      │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 🎬 Summary

This architecture enables:

1. ✅ **Seamless integration** between Keep workflows and OpenAI Assistants
2. ✅ **Stateful conversations** for complex investigations
3. ✅ **Knowledge base access** from workflow steps
4. ✅ **Thread management** for maintaining context
5. ✅ **Error handling** and timeout protection
6. ✅ **Flexible configuration** per workflow

**The provider acts as a bridge** between Keep's workflow engine and OpenAI's advanced Assistants API, making enterprise-grade AI accessible to alert management workflows! 🚀

